import { TaskDoneDirective } from './task-done.directive';

describe('TaskDoneDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskDoneDirective();
    expect(directive).toBeTruthy();
  });
});
